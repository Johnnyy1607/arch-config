# Main configuration for dcli declarative package management
# Bootstrapped from BlackDon's configuration
# Edit this file to customize your system configuration

# Hostname of this machine
host: johnny
# List of enabled modules
# Enable modules with: dcli module enable <module-name>
enabled_modules: []
# Additional packages not in any module
additional_packages: []
# Automatically remove unmanaged packages during sync
auto_prune: false
# Flatpak installation scope: "user" (default) or "system"
flatpak_scope: user
